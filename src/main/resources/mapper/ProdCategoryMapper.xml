<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.output.ProdCategoryMapper">
    <resultMap id="prodCategory" type="com.output.model.ProdCategory">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="prod_id" property="prodId" jdbcType="BIGINT"/>
        <result column="category_name" property="categoryName" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Columns">
        `id`,
        <include refid="Columns_For_Insert"/>
    </sql>

    <!-- Insert使用字段 -->
    <sql id="Columns_For_Insert">
        `prod_id`, `category_name`, `status`, `create_time`, `update_time`
    </sql>

    <!-- 插入数据 -->
    <insert id="insert" parameterType="com.output.model.ProdCategory" useGeneratedKeys="true" keyProperty="id">
        insert into T_PROD_CATEGORY
        (    <include refid="Columns_For_Insert"/>    )
        values(
            #{prodId, jdbcType=BIGINT}, #{categoryName, jdbcType=VARCHAR}, #{status, jdbcType=TINYINT}, 
            #{createTime, jdbcType=TIMESTAMP}, #{updateTime, jdbcType=TIMESTAMP}
        )
    </insert>

    <!-- 批量插入 -->
    <insert id="batchInsert" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        insert into T_PROD_CATEGORY
        (    <include refid="Columns_For_Insert"/>    )
        values
        <foreach collection="list" item="item" separator="," >
            (
                #{item.prodId, jdbcType=BIGINT}, #{item.categoryName, jdbcType=VARCHAR}, 
                #{item.status, jdbcType=TINYINT}, #{item.createTime, jdbcType=TIMESTAMP}, 
                #{item.updateTime, jdbcType=TIMESTAMP}
            )
        </foreach>
    </insert>

    <!-- 根据主键删除 -->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from T_PROD_CATEGORY
        where `id` = #{id, jdbcType=BIGINT}
    </delete>

    <!-- 根据主键更新非空字段 -->
    <update id="updateByPrimaryKey" parameterType="com.output.model.ProdCategory">
        update T_PROD_CATEGORY
        <set>
            <if test=" prodId != null " >
                `prod_id` = #{prodId, jdbcType=BIGINT},
            </if>
            <if test=" categoryName != null and categoryName != '' " >
                `category_name` = #{categoryName, jdbcType=VARCHAR},
            </if>
            <if test=" status != null " >
                `status` = #{status, jdbcType=TINYINT},
            </if>
            <if test=" createTime != null " >
                `create_time` = #{createTime, jdbcType=TIMESTAMP},
            </if>
            <if test=" updateTime != null " >
                `update_time` = #{updateTime, jdbcType=TIMESTAMP},
            </if>
        </set>
        where `id` = #{id, jdbcType=BIGINT}
    </update>

    <!-- 根据主键查询 -->
    <select id="queryByPrimaryKey" parameterType="java.lang.Long" resultMap="prodCategory">
        select <include refid="Base_Columns"/>
        from T_PROD_CATEGORY
        where `id` = #{id, jdbcType=BIGINT}
    </select>

    <!-- 根据条件查询一条记录 -->
    <select id="queryOne" parameterType="com.output.model.ProdCategory" resultMap="prodCategory">
        select <include refid="Base_Columns"/>
        from T_PROD_CATEGORY
        <where>
            <if test=" id != null " >
                AND `id` = #{id, jdbcType=BIGINT}
            </if>
            <if test=" prodId != null " >
                AND `prod_id` = #{prodId, jdbcType=BIGINT}
            </if>
            <if test=" categoryName != null and categoryName != '' " >
                AND `category_name` = #{categoryName, jdbcType=VARCHAR}
            </if>
            <if test=" status != null " >
                AND `status` = #{status, jdbcType=TINYINT}
            </if>
            <if test=" createTime != null " >
                AND `create_time` = #{createTime, jdbcType=TIMESTAMP}
            </if>
            <if test=" updateTime != null " >
                AND `update_time` = #{updateTime, jdbcType=TIMESTAMP}
            </if>
        </where>
    </select>

    <!-- 根据条件查询列表 -->
    <select id="queryList" parameterType="com.output.model.ProdCategory" resultMap="prodCategory">
        select <include refid="Base_Columns"/>
        from T_PROD_CATEGORY
        <where>
            <if test=" id != null " >
                AND `id` = #{id, jdbcType=BIGINT}
            </if>
            <if test=" prodId != null " >
                AND `prod_id` = #{prodId, jdbcType=BIGINT}
            </if>
            <if test=" categoryName != null and categoryName != '' " >
                AND `category_name` = #{categoryName, jdbcType=VARCHAR}
            </if>
            <if test=" status != null " >
                AND `status` = #{status, jdbcType=TINYINT}
            </if>
            <if test=" createTime != null " >
                AND `create_time` = #{createTime, jdbcType=TIMESTAMP}
            </if>
            <if test=" updateTime != null " >
                AND `update_time` = #{updateTime, jdbcType=TIMESTAMP}
            </if>
        </where>
    </select>

    <!-- 统计数量 -->
    <select id="count" parameterType="com.output.model.ProdCategory" resultType="java.lang.Integer">
        select count(*)
        from T_PROD_CATEGORY
        <where>
            <if test=" id != null " >
                AND `id` = #{id, jdbcType=BIGINT}
            </if>
            <if test=" prodId != null " >
                AND `prod_id` = #{prodId, jdbcType=BIGINT}
            </if>
            <if test=" categoryName != null and categoryName != '' " >
                AND `category_name` = #{categoryName, jdbcType=VARCHAR}
            </if>
            <if test=" status != null " >
                AND `status` = #{status, jdbcType=TINYINT}
            </if>
            <if test=" createTime != null " >
                AND `create_time` = #{createTime, jdbcType=TIMESTAMP}
            </if>
            <if test=" updateTime != null " >
                AND `update_time` = #{updateTime, jdbcType=TIMESTAMP}
            </if>
        </where>
    </select>
</mapper>
